#!/usr/bin/make -f
include /usr/share/dpkg/*.mk
export DH_VERBOSE = 1

override_dh_install:
	. ../../build_lib && set_deb_structure "${1c-element-ide}" "${1c-enterprise-element-server-with-ide}" "element-server"

override_dh_strip_nondeterminism:
	true

%:
	dh $@