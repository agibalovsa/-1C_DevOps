# ## OS options

OS_TAG="${OS_TAG}"

# ## Executor options

OC_EXECUTOR_VERSION="${OC_EXECUTOR_VERSION}"
OC_EXECUTOR_TAG="1c/executor:${{OC_EXECUTOR_VERSION}}"

# ## Install options

INSTALL_MODE="${INSTALL_MODE:-install_from_file}"

# ### install_from_file

INSTALL_FILES="1cext-${{OC_EXECUTOR_VERSION}}_all.deb"

# ### install_from_repo

APTLY_REPO_COMPONENTS="1c main"
INSTALL_PACKS="1cext=${{OC_EXECUTOR_VERSION}}"
