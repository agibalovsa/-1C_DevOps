# ## MSSQL Server compose options

# ### names

MSSQL_NAME="${MSSQL_NAME:-mssql}"
MSSQL_TAG="${MSSQL_TAG}"
MSSQL_HOSTNAME="${MSSQL_HOSTNAME:-mssql}"

# ### configs

# > Set when you want to store data on different disks
MSSQL_DATA_MOUNT_PATH="${OC_SRVINFO_MOUNT_PATH}"
MSSQL_TEMP_MOUNT_PATH="${MSSQL_TEMP_MOUNT_PATH}"
MSSQL_BACKUP_MOUNT_PATH="${MSSQL_BACKUP_MOUNT_PATH}"

# ### resource

MSSQL_PORT="${MSSQL_PORT:-1433}"
MSSQL_DATA_VOL="${MSSQL_DATA_VOL:-mssql_data}"
MSSQL_TEMP_VOL="${MSSQL_TEMP_VOL:-mssql_temp}"
MSSQL_BACKUP_VOL="${MSSQL_BACKUP_VOL:-mssql_backup}"
